{
  "name": "object-schema-validate",
  "version": "1.0.3",
  "description": "validates an object according to a schema returning an empty object if valid, otherwise an error object with the same structure",
  "main": "dist/validator.js",
  "scripts": {
    "test": "mocha --reporter spec",
    "build": "babel --out-dir dist --ignore /temp lib"
  },
  "engines": {
    "node": ">=7.5.0"
  },
  "author": "<“Liftit”> (https://github.com/liftitapp)",
  "contributors": [
    {
      "author": "Johaneto",
      "email": "punkeneto1@gmail.com"
    },
    {
      "name": "Angel Botto",
      "email": "angel@liftit.co"
    }
  ],
  "babel": {
    "presets": ["es2015", "stage-2"]
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/liftitapp/object-schema-validate"
  },
  "license": "MIT",
  "keywords": [
    "object-schema-validator",
    "object-schema-validate",
    "js",
    "validation",
    "redux form"
  ],
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "chai": "^4.0.2",
    "mocha": "^3.4.2"
  },
  "dependencies": {
    "check-types": "^7.1.5",
    "is_js": "^0.9.0"
  }
}
